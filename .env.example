# Backend base URL — Better Auth's React client targets `${VITE_API_URL}/api/auth/*`.
VITE_API_URL=http://localhost:5000

# ImgBB key for recipe image uploads
VITE_IMGBB_KEY=your_imgbb_key_here

# Stripe publishable key (client-safe)
VITE_STRIPE_PUBLISHABLE_KEY=your_stripe_publishable_key
